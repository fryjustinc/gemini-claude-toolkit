{
  "name": "@kryonas/gemini-claude-toolkit",
  "version": "1.0.2",
  "description": "A toolkit for Gemini-Claude collaboration, providing an MCP server and initialization command.",
  "main": "dist/index.js",
  "bin": {
    "@kryonas/gemini-claude-toolkit": "dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external=@modelcontextprotocol/sdk --external=@anthropic-ai/claude-code && bun build ./src/server.ts --outdir ./dist --target node --external=@modelcontextprotocol/sdk --external=@anthropic-ai/claude-code && bun build ./src/init.ts --outdir ./dist --target node --external=@modelcontextprotocol/sdk --external=@anthropic-ai/claude-code",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.16.0",
    "@anthropic-ai/claude-code": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "keywords": [
    "gemini",
    "mcp",
    "claude",
    "collaboration",
    "toolkit"
  ],
  "author": "",
  "license": "ISC"
}